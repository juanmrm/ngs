// Scalar values (inmutable)
const answer = 42;
const greeting = 'Hello';

// Arrays and Objects. Constant reference but the content can be mutated
const numbers = [2, 4, 6];
const person = {
  firstName: 'John',
  lastName: 'Doe',
};

//Object.freeze(): inmutability for first level of the object
// Inmutable.js library interesting for this